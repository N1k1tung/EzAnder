#pragma once

const int kColorAnder = 0x7739DE;
const int kColorEnemyHPBar = 0xFF0000;

// support default only for now
const int kWinSizeWidth = 906;
const int kWinSizeHeight = 608;

typedef enum MainRect {
	RectLeft = 45,
	RectTop = 65,
	RectRight = 865,
	RectBot = 620,
};